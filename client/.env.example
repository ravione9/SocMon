# Same-origin (recommended): leave empty so /api and /socket.io go through Vite dev proxy or nginx.
# Direct API: prefer http://127.0.0.1:5000 on Windows + Docker Desktop (localhost / ::1 often resets).
# VITE_API_URL=http://127.0.0.1:5000
VITE_API_URL=
# When using the proxy: backend URL (default http://127.0.0.1:5000). In Docker-only dev with Vite
# inside the same compose network and no VITE_API_URL, set VITE_DEV_PROXY_TARGET=http://server:5000
# VITE_DEV_PROXY_TARGET=
# Optional; empty = socket.io uses the page origin (works with proxy/nginx).
VITE_WS_URL=
VITE_APP_NAME=Lenskart

# Web RDP feature flag (May 2026): currently disabled because libguac/guacd is
# unstable against our Windows Server 2019/2022 hosts (segfaults in libguac.so
# and libc.so.6 across guacd 1.3.0 / 1.5.5 / 1.6.0; reproducible with the
# official Apache Guacamole web client too — confirmed not a Netpulse bug).
# Users see a "use mstsc directly" message in the RDP modal until this is fixed.
# Flip to true and rebuild the client to re-enable when the backend is solved.
VITE_WEB_RDP_ENABLED=false
